# Допустим, что переменная name ссылается на строковое
# значение. Напишите цикл for,
# который напечатает каждый символ в строковом значении.

name = 'Moscow'

for l in name:
    print(l)

print()

print(name[0])